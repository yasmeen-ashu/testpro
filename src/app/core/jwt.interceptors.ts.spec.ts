import { JwtInterceptorsTs } from './jwt.interceptors.ts';

describe('JwtInterceptorsTs', () => {
  it('should create an instance', () => {
    expect(new JwtInterceptorsTs()).toBeTruthy();
  });
});
